# JustACBridge 长期工程契约

## 变更验证硬性要求

- 从本条写入起，任何代码、策略、测试、构建或安装改动都必须由执行改动的 Codex 自行
  完成与改动范围相匹配的验证；不得只凭静态阅读、人工推断或把测试责任留给用户。至少
  应包含针对新增/修复分支的定向测试，并在会影响公共核心、策略注册或推荐源组合时运行
  全量回归测试。测试失败、未能执行或覆盖不足时不得宣称完成，必须先修复或明确报告阻塞。
- 默认验证方式不得启动或连接 WoW，不得读取游戏进程，也不得控制用户键鼠；优先使用
  Lua 语法检查、纯 Lua mock、状态机/边界/故障注入测试和核心集成测试。只有用户明确
  授权实际游戏测试时，才允许把实战验证加入流程；没有实战测试时必须如实保留运行时
  API、secret 值和事件顺序方面的验证边界，不得声称百分之百实战无误。
- 提交或推送前必须确认定向测试和适用的全量测试通过，并执行差异/空白检查；安装后必须
  核对安装版本及部署文件与已测试源码一致，优先使用逐文件哈希或等价的可重复校验。
- 测试不得污染正式源码、游戏目录或提交历史；临时脚本和生成文件必须清理，无关工作区
  文件不得混入提交。最终报告应列出实际执行的测试、结果和仍不能由离线测试证明的风险。

## 推荐源总框架

- 所有循环、爆发保留、技能过滤、优先级和专精行为改动，在设计或实现前必须先查阅
  当前版本成熟高端玩家攻略、权威理论资料或已验证 APL，明确推荐玩法及其适用构筑；
  随后逐项评估所需运行时条件、动作归属和时序能否被 Bridge 可靠观测并通过实战或
  端到端测试验证。只有能够可靠实现的部分才允许落地；任何条件无法可靠取得、规则
  无法完整证明或测试无法覆盖的部分都不得猜测、近似模拟或自行发明优先级，必须保留
  或原样回退当前最新 JustAC 推荐队列。
- 默认由 Bridge 自有推荐源尽最大能力给出当前最优动作。
- 只有更高优先级条件因 secret、API 缺失或运行时证据不足而确实无法可靠判断时，
  才允许原样回退当前最新 JustAC 推荐队列。
- **死命令：自有源不能正面证明自己比 JustAC 更正确时，不得覆盖 JustAC。**任何
  `fallback=true` 路径都必须保留当前 JustAC 队列中剩余动作的原始相对顺序，不得注入
  奥冲或其他技能，不得把低位动作移到 JustAC 首选动作之前。唯一允许的变化是项目契约
  已明确列出的合法性/安全过滤，例如未学习、未绑定、M4 保留、移动/引导、方向/地面
  技能以及奥法 Touch 的同目标凭据；安全过滤只能删除不合法动作并继续读取后续原队列，
  不能借机重写 APL。只有自有规则的全部谓词均被正面证明时，才允许自有动作覆盖队首。
- 不得因为实现困难而过早回退；也不得用猜测、近似状态或不完整谓词冒充精确 APL。
- 自有源已能完整证明的优先级必须由自有源负责；无法证明的动态优先级交回 JustAC。
- `IsSpellUsable`、冷却、充能或高亮结果都不能证明当前角色实际拥有某个技能。任何
  自有推荐源在选择动作前，必须由 `IsPlayerSpell`/`IsSpellKnown` 正面确认技能归属；
  未学习、已移除或不属于当前天赋/英雄树的技能一律不得进入推荐队列。所有导出动作
  还必须具有真实快捷键绑定；未绑定动作必须跳过并继续寻找已绑定动作或专精兜底。

## M5 与 M4 分工

- M5 是正常完整爆发版：执行自有源能可靠证明的完整爆发循环，不因怪物分类自行保留
  爆发；找不到可靠动作时才回退 JustAC。
- M4 是保留爆发版：默认读取原始 JustAC 队列，由核心过滤本专精定义的爆发技能，
  不继承或复制 M5 的自有 APL。唯一例外是 12.1 奥法：M4 与 M5 使用同一个
  `arcane121` 自有优先级，但跳过奥术涌动和大法师之触，再由核心执行按住安全过滤；
  当该源明确回退 JustAC 时，两路都保持安全过滤后剩余动作的 JustAC 原始相对顺序，
  不得注入奥冲或把奥冲移动到 JustAC 推荐的弹幕之前。
- 12.1 奥法默认保留集必须精确为奥术涌动和大法师之触，不合并 JustAC Burst Trigger
  中的其他法术；玩家明确设置的 `/jacb reserve` 覆盖仍优先。
- 12.1 奥法的奥术涌动 `365350` 与大法师之触 `321507` 使用条件配对：涌动明确可用时，
  必须等待触也被正面证明已学习、已绑定、可用且不在冷却，随后先涌动、再在 10 秒内
  释放触；触不就绪时跳过涌动继续普通动作。若涌动被正面证明未学习、未绑定、不可用
  或正在冷却时，M5 只要触明确就绪就直接释放触，不要求上一弹幕/Bolt，也不读取或
  检查涌动剩余冷却阈值。涌动状态未知时不得猜测直放触或先开涌动。
- **12.1 奥法 M5 触死命令：涌动和触同时明确就绪就立即先涌动；涌动成功后下一动作
  直接优先触。涌动只要被正面证明未就绪，触明确就绪就立即直放。**短状态机只有
  `EXPECT_TOUCH` 一步，只接受涌动/触的 `UNIT_SPELLCAST_SUCCEEDED` 建立和完成；触失败、
  中断、出现意外 GCD、目标无效/变化或 10 秒超时都取消。M4 始终保留两技能，不强制
  该配对，也不建立第二份状态。
- 12.1 奥法的涌动配对及上述短状态机凭据必须绑定建立时的敌对
  `targetGUID`。`PLAYER_TARGET_CHANGED`、当前 GUID 不一致、目标死亡或不可攻击都必须
  清除凭据；切回旧目标不得恢复已经清除的凭据。
- M4 必须适合拉怪跑路和普通 Boss 机制期间持续按住：始终跳过不可移动读条、引导和
  蓄力，不因某一帧暂时静止或条件移动施法而放行。12.1 奥法是唯一例外：M4 与 M5
  共用真实移动安全判断，静止时可正常读奥冲/引导飞弹，移动时只有精确满足移动施法
  条件才放行；自有源明确选择的普通动作保持一致，上述 JustAC 回退弹幕保守排序
  同时作用于 M4/M5。
- 12.1 奥法奥术飞弹的唯一移动试放例外：已确认学习浮光掠影、飞弹归属明确、当前
  可用且队列实际推荐飞弹，但节能施法光环不可读时，M4/M5 可在一次连续移动期间试放
  一次。成功后按正常完整引导保护；第一次明确失败后立即锁掉后续试放，直到玩家真实
  停止移动。若节能施法随后能被正面观测到，则精确证据可以解除该失败锁。
- 若 `PLAYER_STOPPED_MOVING` 到达时当前导出动作仍是上述移动试放实例，必须立即取消
  该实例，并让 M4/M5 至少一个完整刷新帧选择队列中的下一安全动作；不能把同一导出
  实例在静止后继续发送。取消只绑定这次试放实例：下一刷新帧重新计算后即使新推荐仍
  是奥术飞弹，也必须作为正常静止推荐处理，不得继承试放取消或失败锁。若速度为
  secret、STOP 尚在防抖确认期，则不得重新建立移动试放；确认静止后才把同 ID 飞弹
  当作新的静止推荐，确认仍在移动或收到新 START 后才允许新的移动试放。

## 按住安全与移动规则

- M5 启动的受保护读条或引导必须立即锁住两个输出，直到真实停止/中断事件；不得用
  猜测时长提前截断。无法可靠区分奥术飞弹截断条件时，完整保护所有飞弹。
- M5 移动时不得推荐不可移动读条；普通专精的 M4 无论当前是否短暂停住都不得启动。
  12.1 奥法 M4 按上一节的专属真实移动规则执行。
- 无法可靠取得目标方位并控制人物面向时，不得自动瞄准方向性投射技能。
- 12.1 奥法奥术宝珠：M4/M5 移动中都保留；普通停止移动后两路都必须连续静止
  2 秒才恢复，加载/重载后的首个静止状态也必须先观察满 2 秒。闪现/闪光术成功施法
  后两路也都等待 2 秒；旧版本保持原行为。
- 12.1 奥法奥术宝珠是多充能技能，自有源不得用“任一充能正在恢复”的
  `IsSpellOnCooldown` 判断整项不可用。必须使用 JustAC 的 secret-safe
  `IsSpellReady` 本地充能追踪：`1/2` 仍可施放，只有 `0/2` 才不可施放；结果未知时
  原样回退 JustAC，不得猜测充能数。

## 专精作用域

- 奥法专属规则只能注册到 `MAGE_1` 对应版本策略和 `arcane121` 推荐源。
- 奥术涌动/大法师之触的 M4 保留、奥术飞弹完整保护、奥术宝珠方向与静止延迟等改动
  不得外溢到其他法师专精、其他职业或通用推荐源。
- 通用核心只提供可复用机制；具体启用项和技能 ID 必须来自当前专精策略。
- 死亡骑士三个专精在默认 `auto` 模式下都必须使用 JustAC 原推荐源；冰/邪 DK 自有源
  只保留为玩家明确执行 `/jacb source frostdk121` 或 `/jacb source unholydk121` 时的
  实验性手动选项，不得自动启用或宣称整体优于 JustAC。DK 的 M4 仍由策略层在 JustAC
  队列上执行爆发保留、方向/地面技能过滤和按住安全规则。
- 玩家按住 DK 的 M4 本身就是“正在跑机制或当前怪群仅剩尾巴”的明确意图信号；不再
  猜测怪物剩余存活时间。M4 保留大爆发、主动饰品/药水及值得留到下一波的短期群怪
  冷却，只从 JustAC 原队列继续执行可移动、无需瞄准的普通填充。冰 DK 使用精确保留
  集：冰霜之柱、冰霜巨龙之怒，以及已学会时必须与冰柱同步的冰龙吐息和死神印记；
  不保留符文武器增效与亡者复生，也不让 JustAC Burst Trigger 把二者重新加入，玩家
  明确设置的 `/jacb reserve` 覆盖仍优先。冷酷严冬 `196770` 继续按 JustAC 普通循环
  释放；12.1 邪 DK 使用精确保留集，只保留亡者大军和黑暗突变，不读取 JustAC Burst
  Trigger 中的旧技能；腐化 `1247378` 是充能制普通循环动作，必须按当前 JustAC 队列
  放行。邪 DK 不为尾怪擅自刷新疾病，灵魂收割 `343294` 也不作为简单 M4 黑名单，
  并继续排除枯萎凋零选点。玩家明确设置的 `/jacb reserve` 覆盖仍优先。
- 死亡之握 `49576` 属于玩家控制的拉怪/位移技能，不是循环动作；DK 三个专精的
  M4/M5 都必须无条件跳过，即使 JustAC 或突进模块把它插入推荐队列。
- 冰 DK 的冰霜巨龙之怒 `279302` 必须严格晚于冰霜之柱 `51271`：只接受玩家成功
  施放冰霜之柱后 10 秒内的近期顺序证据，或当前可明确观测到的冰霜之柱光环；更早
  的历史施法与冷却状态都不能替代当前爆发窗口。未满足时跳过冰霜巨龙之怒并继续读取
  JustAC 后续队列。霜裔天选者把
  按钮明确覆盖为召回法术 `1265384` 时属于二次释放，完全按 JustAC 放行；无法明确
  观测到该覆盖 ID 时不得猜测二次释放状态。
- 冰 DK 的 M4 只能从当前 JustAC 原队列选择动作：JustAC 队列出现凛风冲击 `49184`
  时可直接执行，不另行猜测或证明白霜；队列没有凛风冲击时不得由 Proc、高亮或专精
  兜底自行注入该远程技能。没有其他安全原队列动作时允许 M4 暂时为空。M5 在目标被
  可靠证明处于 5 码外时只允许当前 JustAC 原队列中的凛风冲击：队列没有凛风冲击就
  暂停其他动作与所有注入/兜底，保持为空并跑路；距离未知时不猜测远近，保留原行为。
- 冰 DK 的 M5 在首次冰霜巨龙之怒 `279302` 成功施放后，4 秒内优先接亡者复生
  `46585`。只有亡者复生被明确证明已学会、已绑定、可用且不在冷却时才插入；若明确
  在冷却或冷却状态无法可靠取得，立即放弃该次跟随并继续原 JustAC 队列，不得等待或
  卡住循环。召回覆盖法术 `1265384` 不触发这条首次释放跟随规则，M4 也不启用。
